' Tier 1: Basic VB.NET hello-world. Tests module, Sub Main,
' Console output, and string concatenation.
Module Hello
    Sub Main()
        Dim name As String = "World"
        Console.WriteLine("Hello, " & name & "!")
        Console.WriteLine("VB.NET fixture for RetDec.")
    End Sub
End Module
