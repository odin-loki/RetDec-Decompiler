# Benchmarks

Results are generated by `scripts/run_benchmarks.sh` into `results/<git-sha>.json`.

Regression gates:

- DecompileBench: `scripts/benchmark_regression_gate.sh` vs `results/baseline-2026-08.json`
- Algorithm recovery: `scripts/algorithm_recovery_regression_gate.sh` vs `results/baseline-algorithm-recovery.json`

Auto-generated release table: `docs/BENCHMARKS_TABLE.md` via `scripts/regenerate_benchmark_tables.sh`.

## DecompileBench (pseudocode quality)

Measures recompilation success and syntax validity across optimisation levels.
Compare every release against tag `baseline-2026-08`.

Harness: [tests/decompilebench/README.md](../tests/decompilebench/README.md)

## Algorithm recovery (product differentiator)

Custom metric for `algo_recover`, `sort_detect`, `concurrency_detect`,
`container_detect`, and `serial_detect` modules.

| Component | Path |
|-----------|------|
| Corpus (216+ binaries) | `tests/algorithm_recovery/corpus/` |
| Ground truth | Generated by `build_algorithm_corpus.sh` |
| CI core (9 binaries) | `scripts/run_algorithm_recovery_ci.sh` |
| Full corpus | `scripts/run_algorithm_recovery_full.sh --jobs 4` |
| Nightly | `.github/workflows/algorithm-recovery-nightly.yml` |

Metrics: precision, recall, F1 per class and per binary (`tests/algorithm_recovery/runner.py`).

## Migration evals (steps 28–33)

`bash scripts/migration_eval_suite.sh` — rellic, LIEF, Retypd, SAILR scaffolds.

LLVM inventory: `bash scripts/inventory_llvm_apis.sh` → `results/llvm-api-inventory.json`.

## Honest reporting

Publish weak pseudocode numbers alongside strong algorithm-recovery results.
Parity with stock RetDec on C output is expected under D7 positioning (specification extraction).
