# Claims register (audit E7)

Every externally visible claim needs a demonstrating artefact. A claim
without one is `unpublished` or `withdrawn`. Generate marketing copy from
this table; do not treat README, the whitepaper, or CHANGELOG as evidence.

**Status:** `demonstrated` — artefact in tree · `unpublished` — claimed, no
artefact · `withdrawn` — published then retracted · `opt-in` — off unless
the named flag is set · `asserted` — narrative only (not demonstrated).

**Verification** (BIZ-05): `automated` — CI job or `results/` JSON;
`manual` — read the named artefact; `asserted` — no machine check.

Do not treat `results/algorithm-recovery-*.json` or
`docs/BENCHMARKS_TABLE.md` algorithm rows as current. CHANGELOG `[2.0.21]`
(B4) withdrew those figures after filename-derived detection was removed.

| ID | Claim | Where claimed | Demonstrating artefact | Status | Verification |
|----|-------|---------------|------------------------|--------|--------------|
| C-DB-STOCK | Stock RetDec 5.0 (`remnux/retdec`) DecompileBench JSON exists for the stand-in corpus (CI-core and full) | `CHANGELOG.md` `[2.0.20]`; `docs/BENCHMARKS.md`; `results/README.md` | `results/stock-retdec-docker-ci-core.json`; `results/stock-retdec-docker-full.json` | demonstrated | automated |
| C-DB-COMPARE | Fork vs stock DecompileBench wall/quality two-column compare | `CHANGELOG.md` `[2.0.21]`; `results/compare-fork-vs-stock.md`; `results/compare-fork-vs-stock-full.md` | ci-core 9/9 buildable tu_valid+recompile. Full 216: buildable **1.000**. Debug vs stock Release ~6.3× (ci-core) / ~5.9× (full). Raw `.c` 0. Do not advertise a stock speed win. | demonstrated | manual |
| C-ALGO-F1 | Algorithm-recovery F1 as a product/marketing figure | `README.md`; `docs/BENCHMARKS_TABLE.md`; pre-B1 CHANGELOG | Withdrawn (B4). Name-blind ci-core **0.126**; full 216 name-blind mean **0.056** in `results/algorithm-recovery-full-nameblind.json`. Do not advertise 1.0. | withdrawn | n/a |
| C-B12 | Per-optimisation name-blind F1 as a headline | `RETDEC_AUDIT.md` B12; `docs/BENCHMARKS.md` | `results/algorithm-recovery-per-opt.md`; O0 0.050 / O2 0.059 / O3 0.059. Not a product F1. | demonstrated | manual |
| C-B13 | Micro/macro F1, per-class P/R, bootstrap 95% CI, `n` | `RETDEC_AUDIT.md` B13 | `tests/algorithm_recovery/runner.py`; `results/algorithm-recovery-full-nameblind.json` (`micro`, `macro_f1`, `per_class`, `mean_f1_ci95_*`) | demonstrated | manual |
| C-E1 | Detector labels survive a hashed rename of the input ELF | `RETDEC_AUDIT.md` E1 | `scripts/ci/run_e1_real_binary_smoke.sh`; `results/e1-real-binary-smoke.json`. No F1 gate. Empty detections allowed. | demonstrated | automated |
| C-B6 | CI rename guard: scores unchanged under `$(sha256).bin` | `RETDEC_AUDIT.md` B6 | `scripts/ci/run_b6_rename_guard.sh`; `results/b6-rename-guard.md`. ci-core 9/9 identical. | demonstrated | automated |
| C-B7 | Symbol-name hits tagged and excluded from headline F1 | `RETDEC_AUDIT.md` B7 | Concurrency, open-addressing, named-variant introsort/heapsort/mergesort, name-only partition swap, name-only unordered-map hash, name-only shared_ptr atomic, name-only list alloc, name-only vector growth, serial name-only, and pattern name tables (RAII, Singleton lock, Command execute/undo, Observer subscribe/notify, Factory malloc/new, Strategy doAlgorithm/execute) export tag `evidence:symbol_name`; extract skips them. Extract already drops `std::partition` labels and does not map `kind=="crypto"` / `kind=="serial"` / `kind=="pattern"`. ci-core **0.126**; full 216 **0.056**. `results/b7-name-evidence.md`. AES-NI-below-floor, structural unordered xor+mul, and self-recursion stay leftover. | demonstrated | manual |
| C-B8 | Negative corpus: 200+ binaries with no target-algorithm labels; any hit is a false positive | `RETDEC_AUDIT.md` B8 | `scripts/generate_negative_corpus.py`; `scripts/ci/run_b8_negative_corpus.sh`; `results/b8-negative-corpus.md` | demonstrated | automated |
| C-B8-LOOP | Loop-containing negatives; FP rate on strlen/atoi/dfs/sort heuristics | `RETDEC_AUDIT.md` B8 leftover | `scripts/ci/run_b8_loop_negatives.sh`; `results/b8-loop-negatives.md`. After sort/ring-buffer/copy gates, FP **0.000**. Not a product figure. | demonstrated | automated |
| C-B10 | Third-party binaries labelled from upstream source | `RETDEC_AUDIT.md` B10 | `scripts/build_third_party_corpus.sh` (zlib 1.3.1 pin); `results/b10-third-party.md`. crc32-only 2/2 decompiled, F1 **0.000**. crc+deflate timed out. Not coreutils/OpenSSL/SQLite. | demonstrated | automated |
| C-B9 | Adversarial-positive recall on idiosyncratic implementations | `RETDEC_AUDIT.md` B9 | `scripts/ci/run_b9_adversarial_corpus.sh`; `results/b9-adversarial-positive.md`. Name-blind mean F1 **0.111** on 18 binaries. Not a product figure. | demonstrated | automated |
| C-B11 | Frozen holdout as hashes only | `RETDEC_AUDIT.md` B11 | `tests/algorithm_recovery/holdout/source-hashes.json` (B9 sources). Not a Debian/third-party holdout. | demonstrated | manual |
| C-B16 | Corpus build recipe | `RETDEC_AUDIT.md` B16 | `results/corpus-build-recipe.md`. Host gcc/clang; no pinned container digest. | demonstrated | manual |
| C-Q4 | Existing goto-optimizer baseline measured before any SAILR port | `RETDEC_AUDIT.md` Q4 | `scripts/ci/run_q4_goto_baseline.sh`; `results/goto-optimizer-baseline.md`. ci-core gcc O0/O2/O3: mean **1.44** gotos (O0 still 0). | demonstrated | automated |
| C-A6 | Fibonacci / LCS / Knapsack are not structural detections | `RETDEC_AUDIT.md` A6 | `src/algo_recover/idiom_detect.cpp`; `tests/algo_recover/algo_recover_test.cpp` (`NeverAssignsFibonacciLcsKnapsack`) | demonstrated | manual |
| C-A9 | Design-pattern recovery is experimental heuristics, not a product claim | `RETDEC_AUDIT.md` A9 | `src/pattern_detect/pattern_detector.cpp` file comment | demonstrated | manual |
| C-NEURAL | Optional offline neural refinement (`RETDEC_ENABLE_LLAMACPP` / `RETDEC_NEURAL_REFINE`); compile-gate does not execute C | `README.md`; `docs/NEURAL_REFINEMENT.md`; `docs/COMMERCIAL_WHITEPAPER.md` | `src/neural/gates.cpp` (`cc`/`gcc` argv `-fsyntax-only`); `tests/neural/mock_test.cpp`; `CHANGELOG.md` `[2.0.21]` Security | opt-in | manual |
| C-NEURAL-DIFF | Differential gate compiles and runs decompiled C | `docs/NEURAL_REFINEMENT.md`; `docs/COMMERCIAL_WHITEPAPER.md`; older CHANGELOG | `src/neural/gates.cpp`: `RETDEC_NEURAL_DIFF_GATE` warns and skips; `CHANGELOG.md` `[2.0.21]` Security | withdrawn | n/a |
| C-NEURAL-ALLOW | Model SHA allowlist on load (`support/models.json`); unknown GGUF refused unless `RETDEC_NEURAL_ALLOW_UNVERIFIED` | `RETDEC_AUDIT.md` N6; `support/models.md` | `support/models.json`; `src/neural/model_verify.cpp`; `tests/neural/mock_test.cpp`. Empty allowlist refuses load. Neural itself stays off until `RETDEC_NEURAL_REFINE` | demonstrated | manual |
| C-N11 | Prompt that exceeds `n_ctx` is refused, not silently truncated | `RETDEC_AUDIT.md` N11 | `src/neural/llama_inference.cpp` (`llama_n_ctx` vs tokenize count + `maxTokens`). Piece buffer retry. Context-budget refuse retries once with head/tail source (`summarizeFunctionSource`). The `[truncated for context]` marker is re-emitted after N14 comment stripping. SIGINT/SIGTERM and `RETDEC_NEURAL_DEADLINE_MS` abort generate. GUI Stop already terminate()s the child. | demonstrated | manual |
| C-N12 | Repeat refine is served from a content-addressed cache | `RETDEC_AUDIT.md` N12 | `src/neural/refiner.cpp` (`RETDEC_NEURAL_CACHE_DIR`). Off unless set. Key is SHA-256 of model path/pin, prompt, tier, sampler. `CacheHitReusesAcceptedRefinement`. | opt-in | manual |
| C-N14 | Neural unit tests cover gates, injection strip, context overflow, and manifest schema | `RETDEC_AUDIT.md` N14 | `tests/neural/mock_test.cpp`: spawn-call reject (`system`/`execv`/`ShellExecuteA`/`_popen`/`CreateProcessAsUserA`/`ShellExecuteExA`/`posix_spawn`), string-literal strip, comment-body strip, context-budget retry, `ManifestSchemaHasRequiredKeys`, `EachTierHasDistinctInstruction`, `ConcurrentIndependentRefinesDoNotCrash`. C-source fixtures only; no adversarial-binary injection corpus. Does not claim llama.cpp is thread-safe. | demonstrated | manual |
| C-N15 | Naming-tier GBNF rename map; applyJsonRenameMap skips C keywords | `RETDEC_AUDIT.md` N15 | `src/neural/refiner.cpp` (`namingRenameMapGbnf`, `applyJsonRenameMap`). C11 keywords and spawn-family idents rejected as rename sources/targets. Tests: `ApplyJsonRenameMapRejectsKeywordTarget`, `ApplyJsonRenameMapRejectsSpawnTarget`. Off unless `RETDEC_NEURAL_REFINE`. | opt-in | manual |
| C-N16 | Neural refine prompt includes existing semantic context | `RETDEC_AUDIT.md` N16 | `src/neural/decompile_hook.cpp` (`serializeSemanticContext`): function fields including start/end address, known calling convention, role flags, return/parameter/frame-base storage, parameter real names, and source line numbers, RTTI class members, vtable items (name/address/target/thumb), YARA patterns (including endian and matches), tools (including confidence and significant-nibble counts), languages, architecture (including endian), file format, file class bits, file type, callers/callees, and detection `cHint`. Comments, locals, globals, pattern descriptions, `cElemBytes`, and tool `getAdditionalInfo()` are not dumped. Crypto, serial, and design-pattern detections are now pipeline annotations (`kind="crypto"` / `kind="serial"` / `kind="pattern"`). No invented caller-buffer fields. Tests in `tests/neural/mock_test.cpp`. Off unless `RETDEC_NEURAL_REFINE`. | opt-in | manual |
| C-N17 | Per-suggestion logprob; abstain below threshold | `RETDEC_AUDIT.md` N17 | `src/neural/llama_inference.cpp` (`llama_token_data.p` after `llama_sampler_apply`). Manifest `mean_token_p`. Abstain only if `RETDEC_NEURAL_MIN_MEAN_P` is set. Mock test `LowMeanTokenProbAbstains`. Not per-identifier logprob. | opt-in | manual |
| C-N18 | Whole-program bottom-up refine over the call graph | `RETDEC_AUDIT.md` N18 | `orderFunctionsCalleeFirst` (callee before caller; ties/cycles by address). Two-or-more-function TUs refine per function; accepted callee source is `refined_callees` in the caller prompt. `serializeSemanticContext` still emits `callers`/`callees` from `codeReferences`, including call-graph-only functions. Tests: `CalleeBeforeCaller`, `TiesBrokenByAddress`, `CycleBrokenByAddress`, `AppendRefinedCalleesIntoSemanticJson`, `ExtractsFunctionNamesFromAst`, `SerializesCallGraphOnlyFunctions`. | opt-in | manual |
| C-CUDA-PIPE | CUDA/OpenCL acceleration in the default decompiler pipeline | Historical README / CHANGELOG CUDA backend; `RETDEC_AUDIT.md` II.4 | `CHANGELOG.md` `[2.0.21]` Removed; `docs/CUDA_CAPABILITIES.md`; `src/cuda_accel/README.md` — not linked from `src/retdec` | withdrawn | n/a |
| C-CUDA-OPTIN | Experimental `cuda_accel` / `opencl` exist; `RETDEC_ENABLE_CUDA_ACCEL` default OFF; unintegrated | `README.md` (GPU Acceleration); `docs/CUDA_CAPABILITIES.md`; `docs/COMMERCIAL_WHITEPAPER.md` | CMake option default; `src/cuda_accel/README.md` | opt-in | manual |
| C-EMIT | Buildable sidecars (`.h`, `_stubs.c`, `.buildable.c`) next to output C | `include/retdec/retdec/semantic_recovery_export.h` | `src/retdec/semantic_recovery_export.cpp`; `src/retdec-decompiler/retdec-decompiler.cpp` (`--buildable` / `--no-buildable`); `tests/retdec/semantic_recovery_export_test.cpp`. Default on. Off when `RETDEC_EMIT_BUILDABLE=0` or `--no-buildable`. | demonstrated | manual |
| C-ARCH-UNIMP | SPARC, SystemZ, and XCore lifting are unimplemented | `docs/ARCHITECTURE_TARGETS.md`; `RETDEC_AUDIT.md` T5 | `src/capstone2llvmir/capstone2llvmir.cpp`: `createSparc` / `createSysz` / `createXcore` throw `GenericError` | demonstrated | manual |
| C-LICENCE | Upstream Avast RetDec v5.0 MIT notice retained; Imortek modifications recorded | `CHANGELOG.md` `[2.0.21]` Legal | `docs/PROVENANCE.md`; `LICENSE-MIT`; 2017–2020 rewrite headers restored and CI-gated. File-level classes in `docs/PROVENANCE-files.md` (`LEG-03`). | asserted | automated |
| C-FAST24 | Fast decompile preset ~24% faster, byte-identical output | Historical README (Qt 6 GUI) | none — claim removed from README | withdrawn | n/a |
| C-QWEN3-GPU | In-tree Qwen3 / FlashAttention stack accelerates decompilation | Historical `docs/architecture.md` / whitepaper | not wired into `src/retdec`; docs now say so | withdrawn | n/a |
| C-B15 | Result JSON provenance keys required | `RETDEC_AUDIT.md` B15 | `scripts/ci/verify_result_provenance.py` | demonstrated | automated |
| C-X86-ORACLE | x86 instruction semantics are compared against the real hardware, not against a reading of the manual | `docs/internal/UNFIXED_AUDIT_FINDINGS.md` batches AA–AQ; `CHANGELOG.md` `[Unreleased]` | `scripts/ci/check_x86_flags.sh` (FLAG-01) runs four oracles on the host CPU — `x86_flag_oracle.c`, `x86_wide_oracle.c`, `x86_sse_oracle.c`, `x86_vec_oracle.c` — and compares 13,600 / 17,200 / 15,610 / 27,600 cases against the translator and interpreter. Each has a self-test that corrupts one expected value and requires exactly one mismatch. **x86-64 hosts only**; the other four architectures are not measured this way. | demonstrated | automated |
| C-NO-POISON | The translators emit no shift whose amount can reach the operand width, and no division whose divisor can be zero | `docs/internal/UNFIXED_AUDIT_FINDINGS.md` SHIFT-01 / DIV-01 | `scripts/ci/shift_poison_check.cpp`, run inside `scripts/ci/check_capstone2llvmir_tests.sh`: 83 instructions across five architectures, 179 shifts and 24 divisions, none unsafe. Static — it does not execute the IR, because neither failure is observable by running it. Only the zero divisor is machine-checked; `sdiv INT_MIN, -1` is a relation between two operands and is guarded but not checked. | demonstrated | automated |
| C-C2L-TESTS | Per-instruction translator tests across five architectures | `docs/internal/UNFIXED_AUDIT_FINDINGS.md` | `scripts/ci/check_capstone2llvmir_tests.sh` (C2L-01): 5,800 tests over x86, ARM, ARM64, MIPS and PowerPC, with a per-architecture floor so a suite going missing fails rather than passing quietly. Tests run against `src/llvmir-emul`, whose known remaining gaps are listed in the audit doc — notably that a load's declared width is not honoured. | demonstrated | automated |
| C-OPT-SEMANTICS | The bin2llvmir peepholes preserve what a function computes | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch AS | `scripts/ci/check_bin2llvmir_opts.sh` (OPT-01) with `scripts/ci/inst_opt_semantics_check.cpp`: 36 cases over 810 input points, each evaluated before and after the rewrite with LLVM's own constant folder and required to agree. Self-test replaces every rewrite's answer with its complement and requires all 36 to be reported. **Scope is four passes** — `inst_opt`, `inst_opt_ext`, `strength_reduction`, `redundant_load_store` — not all of bin2llvmir. **It cannot see poison** (the folder ignores `nsw`/`nuw`), anything past one basic block, or a partial overwrite; those are covered by shape in `tests/bin2llvmir/`, which this container cannot execute as a whole. | demonstrated | automated |
| C-BIN2LLVMIR-TESTS | `strength_reduction` and `redundant_load_store` are tested | not claimed externally | `tests/bin2llvmir/optimizations/strength_reduction/` and `.../redundant_load_store/`, newly added and wired into `tests/bin2llvmir/CMakeLists.txt`. Both passes are in the shipped pass list (`src/retdec-decompiler/decompiler-config.json`) and had no tests before this. | demonstrated | automated |
| C-IF2SWITCH-SEMANTICS | The `if`-nest to `switch` reconstruction runs the same body it replaced | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch AX | `tests/llvmir2hll/optimizer/optimizers/if_to_switch_semantics_tests.cpp`: 516 generated nests, 232 of them actually converted, each run before and after the pass for every `v` in `[-12, 40]` and required to select the same body. Falsified against the pass's two families separately — shifting every case value in the 32 deep-matcher sites fails the partition test, shifting the 4 simple/chain sites fails the guard-plus-chain test. **It does not enumerate the forty `tryConvert*` functions**: it demonstrates it reaches the deep matchers, not which of them, because the pass does not report which one fired. Its evaluator models only comparisons of the control variable against a constant and bodies that assign a constant, and refuses anything else rather than passing it. | demonstrated | automated |
| C-IDIOM-PHI | No idiom exchanger rewrites a PHI node | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch AY | `scripts/ci/check_idiom_phi_reach.sh` (IDIOM-PHI-01): all 39 exchangers the per-basic-block dispatcher registers, called on PHI nodes of nine types, 351 pairs, none returning non-null. The exchanger list is read out of `idioms_analysis.cpp` rather than written by hand, and a negative control requires `lshr x, 3` to still be rewritten, so a zero cannot mean the probe called nothing. **It does not exercise `analyse()` itself** — that is private and has no executable coverage in this container. | demonstrated | automated |
| C-IDIOM-USE | An idiom rewrite preserves uses it did not match | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch AZ | `scripts/ci/check_idiom_shared_use.sh` (IDIOM-USE-01): four idiom shapes built twice, the pass run on one, both evaluated over thirteen inputs with LLVM's constant folder and required to agree; `undef` reached only by the rewrite is a failure in its own right. Falsified by reverting the fix, which fails it. **Scope is four shapes in one basic block**, not the 129 erase sites: reverting the fix fails exactly one of the four, so this demonstrates the class rather than enumerating it. Multi-block idioms use the same helper and are not covered. | demonstrated | automated |
| C-LOOP-LOWERING | Lowering a `while true` loop preserves what the function computes | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch BB | `tests/llvmir2hll/optimizer/optimizers/while_true_lowering_semantics_tests.cpp`: the function is run before and after the pass by an interpreter and required to give the same returned value, the same fall-off-the-end, and the same final variable values; iterations are bounded so a non-terminating rewrite is reported rather than hanging. Each of the three fixes falsified separately. **Scope is the three shapes `isLoopEnd` accepts**, for `WhileTrueToUForLoopOptimizer` with one control case through `WhileTrueToWhileCondOptimizer`; the interpreter models integer variables and a fixed set of node kinds and refuses anything else rather than passing it. | demonstrated | automated |
| C-BIN2LLVMIR-RUN | `tests/bin2llvmir` is executed, not merely present | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch BC | `scripts/ci/check_bin2llvmir_tests.sh` (B2L-01): 424 assertions across 35 suites, built against the distribution LLVM and run. **23 of 952 translation units do not compile here** and none is reachable from these tests; they are listed with a reason each and the list is checked in both directions. `src/debugformat/dwarf.cpp` is stubbed, so a test that loaded DWARF would see none; `simplifycfg_tests.cpp` is skipped. The nine `capstone2llvmir` units need the vendored Capstone, so the decoder is not reachable from this harness — `C2L-01` covers the translators separately. | demonstrated | automated |
| C-CONST-FOLD | The backend does not invent constants it cannot derive | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch BD | `tests/llvmir2hll/evaluator/arithm_expr_evaluators/c_arithm_expr_evaluator_tests.cpp` requires a mismatched-width `BitCastExpr` to be refused in both directions and a matching-width one to still fold; `.../llvm_instruction_converter_constants_tests.cpp` requires a GEP offset too big for an int32 not to be truncated. The earlier claim that a *negative* index printed as 4294967295 did not survive falsification and is corrected in the audit record. **Scope is these two conversions**, not constant folding generally. | demonstrated | automated |
| C-LOOP-JUMP | A statement chain is only moved out of its loop when nothing in it jumps to that loop | `CHANGELOG.md` `[Unreleased]`; `docs/internal/UNFIXED_AUDIT_FINDINGS.md` Batch BE | `tests/llvmir2hll/analysis/loop_bound_jump_analysis_tests.cpp`: 12 cases over `LoopBoundJumpAnalysis`, including a `break` after a nested loop and a `continue` inside a `switch`; restoring either defect fails exactly the cases it causes. **It tests the analysis, not its callers** — that a correct answer makes the do-while lowering correct is covered separately and with no nested loops, so the two are each covered and their combination is not. A `GotoStmt`'s target is deliberately not followed and no case builds one. | demonstrated | automated |
| C-ARCH-PARITY | The non-x86 translators are held to the same standard as x86 | not claimed externally | **Not equivalent.** ARM, ARM64, MIPS and PowerPC findings are confirmed against the architecture manuals and capstone's operand tables by reading; this container has no emulator for them, so unlike x86 they are not measured. The audit doc marks each such finding "NOT MEASURED". | asserted | manual |
| C-A12 | Anchor-function selection is "biggest function" | `RETDEC_AUDIT.md` A12 (`retdec.cpp:894-906`) | Stale. Detectors run on every SSA function (`FnWorkItem`); those lines are the incremental cache. | demonstrated | manual |

## How to use

1. Do not republish C-ALGO-F1 or C-CUDA-PIPE. C-LICENCE is **asserted**.
2. Do not fill C-DB-COMPARE until `results/compare-fork-vs-stock.md` exists
   and points at both stock JSON files and a fork run on the same corpus.
3. Opt-in rows are capabilities, not default-on product features.
4. Whitepaper and README must not outrun this register.
